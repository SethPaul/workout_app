{
  "agentCanUpdateSnapshot": true,
  "install": "docker build -f Dockerfile.background-agent -t flutter-bg-agent . && echo 'Flutter Docker image built successfully'",
  "start": "docker run --rm -d --name flutter-dev -p 8080:8080 -v $(pwd):/workspace flutter-bg-agent",
  "terminals": [
    {
      "name": "Flutter Web Dev",
      "command": "docker exec -it flutter-dev flutter run -d web-server --web-port 8080 --web-hostname 0.0.0.0"
    },
    {
      "name": "Test Runner",
      "command": "docker exec -it flutter-dev flutter test --reporter=compact"
    },
    {
      "name": "Docker Logs",
      "command": "docker logs -f flutter-dev"
    }
  ]
}